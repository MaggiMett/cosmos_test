# Event Model

## Purpose

The Event Model defines how changes propagate throughout the Cosmos Runtime.

Events allow Runtime components to react to completed actions without creating direct dependencies.

Events communicate what has happened.

They never request work.

---

# Philosophy

Commands request work.

Services perform work.

Events announce results.

This separation keeps the Runtime predictable, extensible and loosely coupled.

---

# Responsibilities

The Event Model is responsible for:

- notifying Runtime components
- informing subscriber-driven Runtime state synchronization
- supporting subscriber reactions
- enabling extension interoperability
- preserving Runtime consistency

Events never contain business logic.

---

# Event Lifecycle

Every Runtime action follows the same lifecycle.

```text
Command

↓

Runtime Service

↓

Authoritative Permission Validation

↓

Business Validation

↓

Transaction

↓

Persistence

↓

Event Publication

↓

Subscribers

↓

Optional Command or Request to a Runtime Service

↓

Optional Long-Running Job Creation by that Service
```

Events are published only after a successful transaction.

---

# Event Characteristics

Events are:

- immutable
- descriptive
- timestamped
- uniquely identified
- ordered within a transaction

Once published, an Event never changes.

---

# Event Categories

Cosmos initially defines the following Event categories.

## Project Events

Examples:

- ProjectCreated
- ProjectOpened
- ProjectClosed
- ProjectArchived

---

## Workspace Events

Examples:

- WorkspaceOpened
- WorkspaceClosed
- WorkspaceSaved
- WorkspaceChanged

---

## Object Events

Examples:

- ObjectCreated
- ObjectUpdated
- ObjectRenamed
- ObjectDeleted

---

## Knowledge Events

Examples:

- KnowledgeCreated
- KnowledgeUpdated
- KnowledgeVersionCreated
- KnowledgeLinked

---

## Resource Events

Examples:

- ResourceImported
- ResourceModified
- ResourceGenerated
- ResourceDeleted

---

## Relationship Events

Examples:

- RelationshipCreated
- RelationshipRemoved

---

## Runtime Events

Examples:

- ThemeChanged
- ContextChanged
- ExtensionLoaded
- ExtensionActivated

---

## Job Events

Examples:

- JobStarted
- JobProgress
- JobCompleted
- JobFailed
- JobCancelled

---

# Event Consumers

Events may be consumed by:

- System Tools
- User Tools
- Job Runtime
- Companion
- Journeyman
- MCP
- future Extensions

Consumers never modify Events.

---

# Event Schema

Every Event contains:

- Event ID
- Event Type
- Timestamp
- Runtime Context at creation
- Origin Service
- zero or more affected Object IDs
- Metadata

Event Metadata should remain compact.

Large data should be queried through Runtime Services.

Origin Service is visible to subscribers. There is no separate publisher, Runtime Scope or payload schema field; filtering scope is derived from Runtime Context, affected Object IDs and Metadata.

---

# Context

Every Event includes the Runtime Context that existed when the Event was created.

Later Context changes never modify historical Events.

This guarantees reproducibility.

---

# Ordering

Events generated by one transaction preserve their execution order.

The Runtime guarantees deterministic ordering inside a transaction.

Different independent transactions may execute concurrently.

---

# Event Processing

Subscribers react independently.

One failing subscriber must never prevent other subscribers from receiving the Event.

Errors are isolated and reported separately.

A subscriber may react by sending a Command or request to a Runtime Service. The subscriber never treats the Event itself as a Command and never creates a Job directly.

---

# Idempotency

The Runtime provides at-least-once delivery to matching active subscriptions. Duplicate delivery may occur.

Subscribers must safely process the same Event more than once.

Duplicate Event delivery must never corrupt Runtime state.

---

# Persistence

Events are transient Runtime messages.

They are not the permanent source of truth.

Persistent history belongs to:

- Projects
- Objects
- Knowledge
- Resources

Events describe what happened.

They do not become the history themselves.

---

# Long-Running Work

Events never create Jobs or request work.

Example:

KnowledgeCreated

↓

Subscriber

↓

Command or Request to Knowledge Service

↓

Authoritative Validation

↓

Optional Knowledge Processing Job

The Runtime Service decides whether the requested work is long-running and creates the Job when appropriate.

---

# Extensibility

Extensions may subscribe to Events and send Commands through Runtime Services. Runtime Services publish Events for completed actions.

Every Event must:

- describe something that already happened
- remain immutable
- remain independent from consumers

---

# Design Goal

Events should allow Cosmos to grow into a highly connected ecosystem without creating direct dependencies between components.

Every Runtime component should be able to react to changes while remaining completely independent from the implementation of other components.

---

# Principles

- Commands request work.
- Services perform work.
- Events announce results.
- Events never request work or create Jobs.
- Events are immutable.
- Events contain Context.
- Event delivery is at least once and subscribers are idempotent.
- Events never contain business logic.
- Subscribers remain independent.
- One failing subscriber never stops the Runtime.
- Events support unlimited extensibility.
